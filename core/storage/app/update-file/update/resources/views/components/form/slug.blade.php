<div class="single-input">
    <label for="slug" class="label-title mt-3">{{__('Slug')}}</label>
    <input type="text" class="form-control" name="{{ $name ?? '' }}" id="{{ $id ?? '' }}" placeholder="{{__('Slug')}}" id="title">
    <span class="full-slug-show"></span>
</div>
